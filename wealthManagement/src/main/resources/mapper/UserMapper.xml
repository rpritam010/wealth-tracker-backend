<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wealthManagement.wealthManagement.mybatis.mapper.UserMapper">

    <!-- Explicit resultMap to map DB columns to POJO properties -->
    <resultMap id="SampleResponseMap" type="com.wealthManagement.wealthManagement.pojo.responseBody.UserDetailMapper">
        <result property="userId" column="id" />
        <result property="name" column="full_name" />
        <result property="email" column="email" />
        <result property="mobileNumber" column="mobile_number" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <select id="selectOne" resultMap="SampleResponseMap">
        SELECT 
            id,full_name,email,mobile_number,created_at,updated_at
        FROM m_user
    </select>


</mapper>
